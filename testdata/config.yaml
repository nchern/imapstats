# general config section
accounts:
  foo@bar.com:
    INBOX:
      important_count:
        # unseen: true
        headers:
          From: boss@bar.com
      notification_count:
        # seen: true
        headers:
          Subject: "Notification:"
        body:
          - foo
          - bar
      seen_count:
        seen: true
